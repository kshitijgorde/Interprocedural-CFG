The DOT files present in this folder are pruned to reduce dimensionality and to remove statements that contain no useful information. 

The unique system calls identifed in the Pruned DOT files are `497`
